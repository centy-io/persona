pnpm run build || { echo "❌ TypeScript compilation failed. Fix build errors before pushing."; exit 1; }
pnpm run test:coverage || { echo "❌ Tests failed or coverage below 100%. Fix before pushing."; exit 1; }
